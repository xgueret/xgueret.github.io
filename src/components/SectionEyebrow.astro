---
interface Props {
  text: string;
  as?: 'h1' | 'h2' | 'p';
  class?: string;
}

const { text, as: Tag = 'h1', class: className = '' } = Astro.props;
---

<Tag class:list={['text-[0.8125rem] font-medium tracking-[0.01em] text-[var(--color-accent)] m-0', className]}>
  {text}
</Tag>
